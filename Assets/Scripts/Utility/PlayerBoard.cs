using UnityEngine.UI;
using UnityEngine;

public class PlayerBoard : MonoBehaviour {
	public Text nameText, killText, deathText, extraText;
	
	// Use this for initialization
	void Start () {
		
	}
	 
	// Update is called once per frame
	void Update () {
		
	}
}
